module lzbacon.timer;

//Currently skipped, this seems to only serve benchmarking purposes.
//If I'll ever need something similar, I might use some preexisting solution.

/*import core.time;

package class LZHAMTimer{
	static ulong initTicks;
	static ulong freq;
	static double invFreq;

	ulong startTime;
	ulong stopTime;

	bool isRunning;
	bool isStopped;
	enum var_args_t { 
		cVarArgs 
	}
	static this(){

	}
	this(){

	}
	this(ulong initTicks){

	}
	void start(){

	}
	void start(ulong startTime){

	}
	
	void stop(){

	}
	
	@nogc double get_elapsed_secs() const{

	}
	@nogc double get_elapsed_ms() const{ 
		return get_elapsed_secs() * 1000.0f; 
	}
	@nogc ulong get_elapsed_us() const{

	}
	static void init(){

	}
	static @nogc timer_ticks get_ticks_per_sec(){ 
		return g_freq; 
	}
	static @nogc timer_ticks get_init_ticks(){

	}
	static @nogc timer_ticks get_ticks(){

	}
	static @nogc double ticks_to_secs(timer_ticks ticks){

	}
	static @nogc double ticks_to_ms(timer_ticks ticks) { 
		return ticks_to_secs(ticks) * 1000.0f; 
	}
	static @nogc double get_secs() { 
		return ticks_to_secs(get_ticks()); 
	}
	static @nogc double get_ms() { 
		return ticks_to_ms(get_ticks()); 
	}
}

*/